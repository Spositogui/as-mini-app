 <table class="table table-striped table-dark">
  <tbody>
    <% @lists.each do |list| %>
      <tr>
        <td><%= list.title %></td>
        <td><%= "Estado: #{list.status.capitalize}" %></td>
        <td><%= link_to 'Excluir', list, method: :delete, remote: true, class: 'delete btn btn-danger' %></td>
      </tr>
    <% end %>
  </tbody>
</table>