 <table class="table table-striped table-dark">
  <tbody>
    <% @lists.each do |list| %>
      <tr>
        <td><%= list.title %></td>
        <td><%= "Estado: #{list.status.capitalize}" %></td>
        <td>Deletar</td>
      </tr>
    <% end %>
  </tbody>
</table>